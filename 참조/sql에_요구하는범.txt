

실행 할때 마다 다른값을 받아오고싶다 id = ? and name = ?

그 물음표에 대응하는 값을 sql 과 function 사이에 넣어준다
=> ?자리에 data값을 넣어서 문장을 완성시켜 보내준다 (요구할 여러가지 정보를 객체명data로 정리하면 사용하기 편하다)

sql과 통신할 때는 query 문이 받아온 데이터 값을 result에 객체 값으로 변환 해 주지만 client와 통신할때는 Json형식의 string으로 변환하라고 따로 명령해 줘야한다